# Haskell学びメモ 20260608

- whereキーワードを使って計算の中間結果に名前をつける
  - whereキーワードを使うことで条件式で繰り返す使うような値をwhereで定義しておけるみたい。これ欲しかった機能かも。好きかも。。
  - 複数もいけるみたい。ただし、whereの後の変数はインデント揃えてあげないといけない。（ブロックの範囲を正しく認識できずコンパイルエラーになる）
- whereで定義した変数のスコープはガードのみ。下記のようなプログラムはコンパイルエラーになる

```
greet :: String -> String
greet "Juan" = niceGreeting ++ " Juan!"
greet "Fernando" = niceGreeting ++ " Fernando!"
greet name = badGreeting ++ " " ++ name
    where niceGreeting = "Hello! So very nice to see you,"
          badGreeting = "Oh! Pfft. It's you."
```

- whereの束縛の中でもパターンマッチを使うことができる
  - （束縛ってなんだっけ...？）
  - なるほど、引数のfirstname/lastnameから先頭文字だけを抽出したって感じか
  - てか、これパターンマッチっていうんだっけ？

```
initials :: String -> String -> String
initials firstname lastname = [f] ++ ". " ++ [l] ++ "."
    where (f:_) = firstname
          (l:_) = lastname
```

- whereブロックには関数も定義できる
  - ２行目の要素なんだっけと思ったらリスト内包表記か。「元のリストの要素を加工して、新しいリストを作る」時に使う、って説明がわかりやすい。
  - AI引用：数学の集合の定義（例えば $\{x \mid x \in \mathbb{R}\}$ みたいなもの）に似た書き方で、「元のリストの要素を加工して、新しいリストを作る」ときに使います。
  - って考えるとTSでいうmapみたいなものか？

```
calcBmis :: [(Double, Double)] -> [Double]
calcBmis xs = [bmi w h | (w, h) <- xs]
    where bmi weight height = weight / height ^ 2
```

- TypeScriptで書くとこんな感じっぽい。
- なるほど、こう見るとHaskellの書き方はスマートに見えるな
  - つまり縦棒の左側が取り出した要素をどう処理するかの部分（mapの中身）で、右側がどう取り出すかってとこかな

```
const calcBmis = (xs: [number, number][]): number[] => {
  return xs.map(([w, h]) => w / Math.pow(h, 2));
};
```
