# Haskell学びメモ 20260716

- 各桁の数の合計が40になる最初の自然数
- 各桁の　合計を求める関数を作る。ここで技を使う
  - showを使って数を文字列に、それぞれの文字を数に変換してそれを合計する
    - → 文字列は文字のリストだから簡単にできるって感じかな？
  - Data.Char.digitToInt：Charを受け取りIntを返す
    - '0'~'9', 'A'~'F'(小文字でも可)の文字に対して動作する

```
ghci> digitToInt '2'
2
ghci> digitToInt 'F'
15
ghci> digitToInt 'z'
*** Exception: Char.digitToInt: not a digit 'z'
```

- find：述語関数とリストを引数に取り、リストの中で条件に合致する最初の要素を返す
  - この関数は型が変わっている
  - 返り値のMaybe aとは？
    - 0個かちょうど１個の要素だけを持てる
    - この型は失敗する可能性があることを表現するのに使う
      - 何も持っていないという値を作るにはNothingを使う

```
ghci> :t find
find :: Foldable t => (a -> Bool) -> t a -> Maybe a
```

- Just Trueの型はMaybe Bool、これは真理値を保持するリストの型が[Bool]であるようなこと

```
ghci> Nothing
Nothing
ghci> Just "hey"
Just "hey"
ghci> Just 3
Just 3
ghci> :t Just "hey"
Just "hey" :: Maybe String
ghci> :t Just True
Just True :: Maybe Bool
```

- findで述語を満たす要素が見つかったら、その要素をJustでラップしたものが返される。見つからなければNothingが返される

```
ghci> find (> 4) [3,4,5,6,7]
Just 5
ghci> find odd [2,4,6,8,9]
Just 9
ghci> find (=='z') "mjolnir"
Nothing
```

- 引数としてもっと一般的な関数が欲しい場合は簡単に変更可能

```
ghci> firstTo40
Just 49999
ghci> firstTo40
Just 49999
ghci> :l study-memo/20260716/20260716.hs
ghci> firstTo 27
Just 999
ghci> firstTo 1
Just 1
ghci> firstTo 13
Just 49
```
