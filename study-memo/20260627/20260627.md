# Haskell学びメモ 20260628

- flip：関数を引数にとり、元の関数と似ているけど最初の2つの引数が入れ替わった関数を返す
  - 型宣言から「a型とb型の値を引数に取る関数」を引数にとり、「b型とa型の値を引数に取る関数」を返す
  - なお関数はデフォルトでカリー化されてるので2つ目の括弧は不要
    - へーそうか。言われてみればだけど可読性的には合った方が良いな

```
flip' :: (a -> b -> c) -> (b -> a -> c)
flip' f = g
    where g x y = f y x
```

- `(a -> b -> c) -> (b -> (a -> c))`、`(a -> b -> c) -> b -> a -> c`とも書ける
- となると下記のようにも書ける
  - いまいちよくわからん

```
flip' :: (a -> b -> c) -> b -> a -> c
flip' f y x = f x y
----
ghci> zipWith' (zipWith' (*)) [[1,2,3],[3,5,6],[2,3,4]] [[3,2,2],[3,4,5],[5,4,3]]
[[3,4,6],[9,20,30],[10,12,12]]
ghci> :l study-memo/20260627/20260627.hs
[1 of 2] Compiling Main             ( study-memo/20260627/20260627.hs, interpreted )
Ok, one module loaded.
ghci> zip [1,2,3,4,5] "hello"
[(1,'h'),(2,'e'),(3,'l'),(4,'l'),(5,'o')]
ghci> flip' zip [1,2,3,4,5] "hello"
[('h',1),('e',2),('l',3),('l',4),('o',5)]
ghci> zipWith div [2,2..] [10,8,6,4,2]
[0,0,0,0,1]
ghci> zipWith (flip' div) [2,2..] [10,8,6,4,2]
[5,4,3,2,1]
```

メモ：やっていることは同じで、引数を入れ替えるタイミングが違うだけです。

- 1つ目: 「入れ替え済みの関数」を先に作って返す
- 2つ目: 呼ばれたときに引数を入れ替えて渡す
